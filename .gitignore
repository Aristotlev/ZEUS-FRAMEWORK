venv/
__pycache__/
*.pyc
*.pyo
.env
.env.local
.env.prod
deploy/.env.prod
*.egg-info/
node_modules/
.pytest_cache/
dist/
build/
*.pem
*.ppk
logs/
data/
wandb/
.DS_Store
# EVENT_CLIP manual-drop inbox — bind-mounted into the container on the
# prod VM. Holds raw mp4/mov/mkv files scp'd by the user. Never committed.
/event_clip_inbox/
# Claude Code subagent worktrees — local session scratch space, never commit
.claude/worktrees/
# Claude Code per-user local settings — leaks usernames + local paths
.claude/settings.local.json
.claude/settings.*.local.json
